A django app for demonstrating webpdb

To use:

pip install -r requirements.txt
python manage.py runserver

Then visit http://localhost:8000

the page will appear to hang

Open a new tab at http://localhost:5555 to get to the webpdb interface

More on webpdb here https://github.com/romanvm/python-web-pdb
